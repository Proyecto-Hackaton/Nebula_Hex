{
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm config set prefer-frozen-lockfile false && pnpm install",
  "buildCommand": "pnpm build",
  "outputDirectory": ".next"
}